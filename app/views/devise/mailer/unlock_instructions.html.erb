<p><%= t('devise.mailer.hello', email: @resource.email)%></p>

<p><%= t('devise.mailer.unlock_instructions.message') %></p>

<p><%= link_to t('devise.mailer.unlock_instructions.link'), unlock_url(@resource, unlock_token: @token) %></p>
