<p><%= t('devise.mailer.hello', email: @email) %></p>

<p><%= t('devise.mailer.confirmation_instructions.message') %></p>

<p><%= link_to t('devise.mailer.confirmation_instructions.submit'), confirmation_url(@resource, confirmation_token: @token) %></p>
